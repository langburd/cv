{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; echo \"$f\" | grep -qE '\\.(md|html|yml|css)$' && jekyll build 2>&1 | tail -3; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "jekyll build..."
          }
        ]
      }
    ]
  }
}
